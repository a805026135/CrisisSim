# 截图指引

请按照以下编号和描述截取对应界面的截图，保存到本目录下。

---

## 01-overview-hero.png
**位置**：README 顶部标题下方
**截什么**：应用运行后的**首页全貌**，能看到暗色主题的完整界面布局
**建议**：选择一个场景并点击进入后，截取包含场景标题和 Agent 列表的完整页面，作为项目"第一印象图"

---

## 02-features-overview.png
**位置**：「核心功能」表格下方
**截什么**：展示系统**主要操作界面的拼图**，体现功能丰富度
**建议**：可以将以下 2-3 个画面拼成一张图：
- 左上：场景选择页面
- 右上：策略生成（含 SSE 流式输出效果）
- 下方：仿真完成后的情绪图表
（也可以只截一张最有代表性的界面）

---

## 03-architecture-diagram.png
**位置**：「整体架构图」ASCII 图下方
**截什么**：一张**技术架构流程图**
**建议**：可以用 draw.io / Excalidraw / ProcessOn 等工具画，也可以截取代码中的架构图渲染效果。内容应体现：前端 → API → 仿真引擎 → Agent层 → LLM / RAG 的调用关系

---

## 04-scenarios-overview.png
**位置**：「预设场景详解」标题下方
**截什么**：5 个预设场景的**选择界面**
**建议**：截取应用启动后的场景选择页面，能同时看到 5 个场景卡片/选项的全貌

---

## 05-startup-page.png
**位置**：「安装与运行」代码块下方
**截什么**：首次打开 `http://localhost:5000` 的**初始页面**
**建议**：刚启动服务、尚未选择场景时的干净首页，展示应用的初始状态

---

## 06-scenario-select.png
**位置**：「Step 1: 选择危机场景」段落下
**截什么**：选中某个场景后的**场景详情展示**
**建议**：选择「喜茶奶茶食品安全事件」（或其他任一场景），截取加载完成后的页面，能看到场景标题、简介和 Agent 列表

---

## 07-agent-config.png
**位置**：「Step 2: 配置 Agent 角色」段落下
**截什么**：Agent 角色的**编辑配置界面**
**建议**：展开某个 Agent 的详情卡片，能看到立场滑块、影响力滑块、发言风格、角色描述等编辑区域。如果支持添加自定义 Agent，也截取该操作入口

---

## 08-strategy-select.png
**位置**：「Step 3: 策略推演」段落下
**截什么**：AI 公关顾问生成的**两套候选策略**
**建议**：截取策略 A 和策略 B 的卡片并排展示，能看到策略名称、描述、理由和官方声明草稿。如果正在 SSE 流式输出，截取输出过程中的动态效果更佳

---

## 09-simulation-running.png
**位置**：「Step 4: 实时观察仿真过程」段落下
**截什么**：仿真执行中的**消息时间线 + 数据面板**
**建议**：等一轮仿真完成后截取，画面应包含：
- 左侧/上方：各 Agent 的反应消息列表（带角色类型标签和情绪标签）
- 右侧/下方：情绪分布指标和立场演化图表
（根据实际 UI 布局截取，尽量完整）

---

## 10-analysis-dashboard.png
**位置**：「Step 5: 多轮迭代与复盘」段落下
**截什么**：多轮仿真完成后的**分析仪表盘**
**建议**：完成 2-3 轮推演后截取，画面应包含：
- 情绪趋势折线图（正面/负面/中立 3 条线）
- Agent 立场演化折线图（多条线，每条代表一个 Agent）
- PR 顾问的反思文字
- 每轮策略效果汇总
